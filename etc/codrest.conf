# This is really -*-python-*-

# Address and port to listen on, if run as standalone server
host = '0.0.0.0'
port = 8303

# Path to database directory
database = '/var/lib/codplayer'

# List of players to show in the admin UI
players = [
    { 'name': 'Example player',
      'url': 'http://cod:8304/',
      'default': True
      },
    ]

